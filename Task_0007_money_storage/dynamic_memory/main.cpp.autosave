#include <iostream>
#include <memory.h>
#define WORD_LENGTH 20


using namespace std;

int main()
{
    char * string_in = new char[WORD_LENGTH];
    memset(string_in, 0, WORD_LENGTH * sizeof(char));
            
    gets(string_in);
    return 0;
}

